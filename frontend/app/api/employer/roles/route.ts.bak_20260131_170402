import { proxy } from "../../_proxy";

export async function GET(req: Request) {
  return proxy(req, "/employer/roles");
}

export async function POST(req: Request) {
  return proxy(req, "/employer/roles");
}